import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#a21caf",
    borderRadius: 5,
    paddingVertical: 10,
    alignItems: "center",
  },
  buttonTitle: {
    fontSize: 18,
    fontWeight: "600",
    color: "#f1f5f9",
  },
});
